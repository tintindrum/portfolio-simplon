<nav id="main-nav" class="navbar navbar-expand-lg navbar-light bg-charte-terre">
    
    <div class="container-fluid">

   
        {% if true %}
            <a class="navbar-brand mx-2 d-flex flex-column" href="{{ path('app_main') }}">
                <img class="quentin-round" height="50" width="50" src="{{asset('img/logoqd.jpg')}}" alt="Projet examen Simplon">
                <small class="sil2 verysmall fw-bold text-center mt-1 text-capitalize text-black">Projet Examen<br> Simplon</small>
            </a>
        {% elseif logo_id is not defined or not logo_id %}
        <a class="navbar-brand" href="{{ path('app_main') }}">{{ SITE_NAME }}</a>
        {% else %}
            <a class="navbar-brand mx-2" href="{{ path('app_main') }}"><img height="80" src="{{asset('Final/'~logo_id~'.png')}}" alt="Projet examen Simplon"></a>
        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 flex-grow-1 gap-3">
                
                <li class="nav-item flex-grow-1 mx-2">
                    <form class="d-flex">
                        <div class="input-group mx-2 searchnav">
                            <div class="form-floating flex-grow-1">
                            <input class="form-control" type="search" placeholder="Qu'est ce qui vous ferait plaisir ?" aria-label="Search" id="search">
                                 <label for="search" class="text-muted">{# {{ reglages_service.get('invite de recherche') }} #}Vous recherchez quoi ?</label>
                            </input>
                            </div>
                            <button class="main-search-button btn text-black" type="submit">
                                <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="currentColor" class="bi bi-search-heart">
                                    <use xlink:href="#svg-search-heart"></use>
                                </svg>
                            </button>
                        </div>
                    </form>
                </li>


                 <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex flex-column align-items-center small gap-1 mashco-children-no-margin text-black" href="{# {{ path('app_profil') }} #}"
                       id="navbarDropdownProfil" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-person-circle">
                            <use xlink:href="#svg-person-circle"></use>
                        </svg>

                        Admin
                    </a>

                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownProfil">
                     {#    {% if app.user %} #}
                        <li class="nav-item">
                            <a class="nav-link" href="{# {{ path('app_profil') }} #}">Dashboard</a>
                        </li>
                       {#  {% if is_granted('ROLE_ADMIN') %} #}
                            <li class="nav-item">
                                <a class="nav-link" href="{# {{ path('admin') }} #}">Admin</a>
                            </li>
                       {#  {% endif %} #}
                        
                    </ul>
                </li>


            

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex flex-column align-items-center small gap-1 quentin-children-no-margin text-black" href="{# {{ path('app_profil') }} #}"
                       id="navbarDropdownProfil" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-person-circle">
                            <use xlink:href="#svg-person-circle"></use>
                        </svg>

                        Profil
                    </a>

                   <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownProfil">
                    {# {% if app.user %} #}
                        <li class="nav-item">
                            <a class="nav-link" href="{# {{ path('app_profil') }} #}">Profil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{# {{ path('app_logout') }} #}">Se deconnecter</a>
                        </li>
                    {# {% else %} #}
                        <li class="nav-item">
                            <a class="nav-link" href="{# {{ path('app_login') }} #}">Se connecter</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{# {{ path('app_register') }} #}">S'inscrire</a>
                        </li>
                    {# {% endif %} #}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
