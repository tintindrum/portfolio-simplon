{% set article_show = path('article_show', {'slug': article.slug}) %}

{# <article class="mb-5">
    <div class="row">

        <div class="col-md-5">
        {% if article.featuredImage %}
            <a href="{{ article_show }}">
                <img src="/upload/{{ article.featuredImage.filename }}" alt="{{ article.featuredImage.filename.altText }}" loading="lazy" width="350" height="250">
            </a>
            
        {% endif %}

        </div>

        <div class="col-md-7">
            <h2>
                <a class="text-decoration-none" href="{{ article_show }}">{{ article.title }}</a>
            </h2>

            <p>
                {{ article.createAt|date('d M Y')}}
            </p>

            {{ article.featuredText ?: article.content|striptags|slice(0, 130) ~ '...' }}
        
        
        </div>
    </div>

</article>
 #}

{# card caroussel projets  #}
<div class="u-s-m-b-30">
    <div class="product-o product-o--hover-on">
        <div class="product-o__wrap">

            {% if article.featuredImage %}
                <a href="{{ article_show }}">
                    <img
                        src="/upload/{{ article.featuredImage.filename }}"
                        alt="{{ article.featuredImage.filename.altText }}"
                        loading="lazy"
                        width="350"
                        height="250">
                </a>

            {% endif %}

            <div class="product-o__action-wrap">
                <ul class="product-o__action-list">

                    <li>

                        <a
                            data-modal="modal"
                            data-modal-id="#add-to-cart"
                            data-tooltip="tooltip"
                            data-placement="top"
                            title="Add to Cart">
                            <i class="fas fa-plus-circle"></i>
                        </a>
                    </li>

                </ul>
            </div>
        </div>

        <span class="product-o__category">

            <a href="shop-side-version-2.html">Catégories</a>
        </span>

        <span class="product-o__name">
            <a class="text-decoration-none" href="{{ article_show }}">{{ article.title }}</a>
        </span>

        <p>
            {{ article.createdAt|date('d M Y')}}
        </p>

    </div>
</div>

{# end fin card card caroussel #}